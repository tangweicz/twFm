1、设置编译之后的可执行文件路径

    在 build setting > build Location > pre-configuration build products path > debug

2、头文件设置路径

    在 build setting > header search paths

3、库文件搜索路径

    在 build setting > library search paths

4、@rpath的设置

    在 build setting > runpath search paths